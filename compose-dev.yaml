version: '3'
services:
  node_app:
    platform: linux/arm64
    build:
      context: .
    ports:
      - 1234:1234
    volumes:
      - ~/.ssh/ionos:/com.docker.devenvironments.code/auth/sshkey
      - ./app:/app
